[Unit]
Description=Cap2 FastAPI App
After=network.target

[Service]
WorkingDirectory={{ app_dir }}
ExecStart={{ app_dir }}/venv/bin/uvicorn app.main:app --host 0.0.0.0 --port {{ app_port }}
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target